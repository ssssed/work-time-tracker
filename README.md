# ⏱️ Work Time Tracker (WTT) --- v2.0.0

**Work Time Tracker (WTT)** --- CLI‑утилита для автоматического
отслеживания времени, проведённого в git‑ветках каждого проекта.\
Версия **2.0.0** включает переработанную систему хранения данных,
улучшенную работу фоновых процессов и новые команды.

------------------------------------------------------------------------

# 🔥 BREAKING CHANGES (v2.0.0)

Версия **2.0.0** содержит изменения, которые могут нарушить работу
предыдущих установок.

### ❗ 1. Новое место хранения данных

Теперь все файлы WTT хранятся в директории:

    ~/wtt/

Структура:

    ~/wtt/wtt.json                 — основная БД проектов  
    ~/wtt/.wtt.log                 — лог‑файл  
    ~/wtt/.wtt.<project>.pid       — PID‑файлы демонов

Раньше файлы находились непосредственно в `~`, например:

    ~/.wtt.projectName.pid
    ~/.wtt.log

### ❗ 2. Старые PID‑файлы больше не используются

Если вы обновились поверх старой версии --- старые PID‑файлы в `~` могут
остаться, но WTT их игнорирует.

### ❗ 3. Поведение `wtt start` изменено

Теперь фоновый процесс корректно отделяется и использует собственный
лог‑файл.

------------------------------------------------------------------------

# 🚀 Установка

``` bash
npm install -g work-time-tracker
```

После установки команда `wtt` будет доступна глобально.

------------------------------------------------------------------------

# 📘 Основные команды

## ▶️ `wtt start`

Запускает отслеживание времени в текущем git‑проекте.

``` bash
wtt start
```

Можно запустить в foreground-режиме:

``` bash
wtt start --foreground
```

По умолчанию процесс работает **в фоне**.

------------------------------------------------------------------------

## 🛑 `wtt stop`

Останавливает отслеживание времени в проекте.

``` bash
wtt stop
```

Выбирается активный демон, процесс завершается, PID‑файл удаляется.

------------------------------------------------------------------------

## 🧩 `wtt process:list`

Показывает список всех активных демонов WTT:

``` bash
wtt process:list
```

------------------------------------------------------------------------

## 💣 `wtt process:kill`

Принудительно завершает **все** процессы WTT.

``` bash
wtt process:kill
wtt process:kill --force
```

------------------------------------------------------------------------

## 📊 `wtt view`

Просмотр статистики по проектам и веткам.

``` bash
wtt view
```

Флаги:

  Флаг                        Описание
  --------------------------- ----------------------------------------
  `-d, --date <dd-mm-yyyy>`   показать статистику за конкретную дату
  `-a, --all`                 показать статистику по всем проектам
  `-t, --table`               отобразить в виде таблицы

------------------------------------------------------------------------

## 🧹 `wtt clear`

Удаляет все сохранённые данные WTT.

``` bash
wtt clear
```

------------------------------------------------------------------------

# 📂 Директория хранения (v2.0.0)

    ~/wtt/
        wtt.json
        .wtt.log
        .wtt.<project>.pid

------------------------------------------------------------------------

# 🛠️ Стек

-   Node.js + TypeScript
-   chalk
-   inquirer
-   commander
-   dayjs

------------------------------------------------------------------------

# ❤️ Пример рабочего цикла

``` bash
cd ~/projects/my-app
wtt start
# ... работаешь ...
wtt stop
wtt view
```
