# ⏱️ Work Time Tracker (WTT)

**Work Time Tracker (WTT)** - CLI-утилита для отслеживания времени, проведённого в разных git-ветках каждого проекта.  
Утилита фиксирует активность по времени, автоматически обновляет счётчик и сохраняет статистику по дням.

---

## 🚀 Установка

```bash
npm install -g work-time-tracker
```

После установки команда `wtt` будет доступна глобально в терминале.

---

## 📘 Основные команды

### ▶️ `wtt start`

Запускает отслеживание времени в текущем git-проекте.

```bash
wtt start
```

По умолчанию процесс запускается **в фоне**.  
Если нужно запустить **в режиме блокировки терминала**, добавьте флаг:

```bash
wtt start --foreground
```

---

### 🛑 `wtt stop`

Останавливает отслеживание для выбранного проекта.  
При запуске появляется список всех активных процессов с выбором проекта.

```bash
wtt stop
```

> 💡 После выбора проект будет остановлен, а его PID-файл удалён.

---

### 🧩 `wtt process:list`

Показывает список всех активных фоновых процессов `wtt start`:

```bash
wtt process:list
```

Пример вывода:

```
Активные процессы wtt start:
sssed     64292   0.0  0.2  node /Users/.../dist/demons/start.js
sssed     62042   0.0  0.2  node /Users/.../dist/demons/start.js
```

---

### 💣 `wtt process:kill`

Принудительно завершает **все активные** процессы WTT.

```bash
wtt process:kill
```

Можно использовать с флагом `--force` для пропуска подтверждения:

```bash
wtt process:kill --force
```

---

### 📊 `wtt view`

Отображает накопленную статистику времени по веткам и проектам.

```bash
wtt view
```

Дополнительные опции:
| Опция | Описание |
|-------|-----------|
| `-d, --date <dd-mm-yyyy>` | Показать статистику за конкретную дату |
| `-a, --all` | Показать статистику по всем проектам |
| `-t, --table` | Показать статистику в виде таблицы |

Пример:

```bash
wtt view --date 01-11-2025
```

---

### 🧹 `wtt clear`

Удаляет все сохранённые данные об активности.

```bash
wtt clear
```

Для удаления без подтверждения:

```bash
wtt clear --force
```

---

## 🛠️ Пример рабочего цикла

```bash
cd ~/projects/auto-builds
wtt start          # запустить отслеживание в фоне

# ... работаешь над проектом ...

wtt stop           # остановить процесс
wtt view           # посмотреть отчёт за сегодня
```

---

## 📂 Где хранятся данные

- Все временные PID-файлы процессов:  
  `~/.wtt.<projectName>.pid`

- История времени по проектам хранится в JSON-файлах через `StorageService`.

---

## ⚙️ Технические детали

- Написано на **Node.js + TypeScript**
- Используются:
  - [`dayjs`](https://day.js.org/) — для форматирования дат
  - [`chalk`](https://github.com/chalk/chalk) — для цветного вывода в консоль
  - [`inquirer`](https://github.com/SBoudrias/Inquirer.js/) — для интерактивного выбора
  - [`commander`](https://github.com/tj/commander.js/) — для CLI-команд

---
